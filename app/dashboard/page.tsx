import {redirect} from "next/navigation";import {getMerchantContext} from "@/lib/auth/supabaseAuth";import {getOrganizationProfile} from "@/lib/merchant/profile";export const metadata={title:"Merchant dashboard"};export default async function Page(){const c=await getMerchantContext();if(!c)redirect("/login");if(!c.subscription)redirect("/choose-plan");if(!["active","trialing"].includes(c.subscription.status))redirect("/checkout");if(!c.organization.onboarding_complete)redirect("/onboarding/business");const profile=await getOrganizationProfile(c.organization.id);const location=[profile.location?.city,profile.location?.state].filter(Boolean).join(", ")||"Primary location";return <main className="merchant-dashboard"><aside><a className="dashboard-brand" href="/">VORDALI <small>COMMIT</small></a><nav><a className="active" href="/dashboard">Overview</a><span>Payment requests</span><span>Customers</span><span>Analytics</span><a href="/billing">Billing</a><a href="/settings/business">Business settings</a></nav><form action="/api/auth/logout" method="post"><button>Sign out</button></form></aside><section><header><div><p className="kicker">Merchant dashboard</p><h1>Welcome, {c.organization.name}.</h1><p>{location} · {c.subscription.plan_slug} plan</p></div><button className="button button-primary" disabled>Create payment request</button></header><div className="dashboard-status"><article><span>Subscription</span><strong>{c.subscription.status}</strong><small>{c.subscription.plan_slug} plan</small></article><article><span>Business setup</span><strong>Complete</strong><small>{profile.location?.name||"Primary location"}</small></article><article><span>Today&apos;s requests</span><strong>0</strong><small>Payment-request tools arrive next</small></article><article><span>Today&apos;s revenue</span><strong>$0</strong><small>No completed payments yet</small></article></div><section className="dashboard-welcome"><p className="kicker">Account activated</p><h2>Your merchant workspace is ready.</h2><p>Your subscription, business profile, primary location, and payment-link defaults are now connected. The next sprint activates the Create Payment Request button.</p><div className="dashboard-actions"><a className="button button-secondary" href="/settings/business">Review business settings</a><a className="button button-secondary" href="/billing">Manage billing</a></div></section></section></main>}
