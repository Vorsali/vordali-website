import { redirect } from "next/navigation";import { getMerchantContext } from "@/lib/auth/supabaseAuth";export const metadata={title:"Merchant dashboard"};export default async function Page(){const c=await getMerchantContext();if(!c)redirect("/login");if(!c.subscription)redirect("/choose-plan");if(!["active","trialing"].includes(c.subscription.status))redirect("/checkout");if(!c.organization.onboarding_complete)redirect("/onboarding/business");return <main className="merchant-dashboard"><aside><a className="dashboard-brand" href="/">VORDALI <small>COMMIT</small></a><nav><a className="active" href="/dashboard">Overview</a><span>Payment requests</span><span>Customers</span><span>Analytics</span><span>Settings</span></nav><form action="/api/auth/logout" method="post"><button>Sign out</button></form></aside><section><header><div><p className="kicker">Merchant dashboard</p><h1>Welcome, {c.organization.name}.</h1></div><button className="button button-primary" disabled>Create payment request</button></header><div className="dashboard-status"><article><span>Subscription</span><strong>{c.subscription.status}</strong><small>{c.subscription.plan_slug} plan</small></article><article><span>Business setup</span><strong>Complete</strong><small>Ready for payment tools</small></article><article><span>Today&apos;s requests</span><strong>0</strong><small>Request tools arrive next</small></article><article><span>Today&apos;s revenue</span><strong>$0</strong><small>No completed payments yet</small></article></div><section className="dashboard-welcome"><p className="kicker">Foundation active</p><h2>Your merchant account is ready.</h2><p>The next sprint connects subscription billing and then unlocks the payment-request workflow.</p></section></section></main>}
