import { PageShell } from "@/components/PageShell";import { PlanChooser } from "@/components/merchant/PlanChooser";import { getCurrentUser } from "@/lib/auth/supabaseAuth";export const metadata={title:"Choose your plan"};export default async function Page(){const user=await getCurrentUser();return <PageShell><main className="merchant-flow-page"><header><p className="kicker">Step 1 of 3</p><h1>Choose the plan that fits your business.</h1><p>Your plan is confirmed before dashboard access. You can change tiers later from billing settings.</p></header><PlanChooser signedIn={!!user}/></main></PageShell>}
