"use client";import { FormEvent,useState } from "react";import { PageShell } from "@/components/PageShell";export default function Page(){const[sent,setSent]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});setSent(true)}return <PageShell><main className="merchant-auth-page"><section className="merchant-auth-copy"><p className="kicker">Account recovery</p><h1>Reset your password.</h1><p>We will send recovery instructions if an account exists for that address.</p></section><form className="merchant-form" onSubmit={submit}><label>Email<input name="email" type="email" required/></label>{sent&&<p className="form-success">Check your email for recovery instructions.</p>}<button className="button button-primary">Send reset link</button></form></main></PageShell>}
