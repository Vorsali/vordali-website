# Classic Recovery Tank visibility fix

- Assigned unique SVG definition IDs to every SignatureRecoveryTank instance.
- Prevents duplicate gradient, clip-path, and filter IDs when the Hero tank and homepage Classic preview render together.
- No changes were made to the Restaurant Kitchen Series vessel.
