"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";
type Props={profile:{name:string;phone:string|null;website:string|null;timezone:string;support_email:string|null;default_request_expiration_minutes:number};location:{name:string;address_line1:string|null;address_line2:string|null;city:string|null;state:string|null;postal_code:string|null}|null;submitLabel?:string};
export function BusinessOnboardingForm({profile,location,submitLabel="Complete business setup"}:Props){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const r=await fetch("/api/merchant/business",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const result=await r.json();setBusy(false);if(!r.ok)return setError(result.error||"Unable to save.");router.push(result.next);router.refresh();}return <form className="merchant-form merchant-form-wide" onSubmit={submit}><div className="form-section"><p className="kicker">Business identity</p><div className="form-grid"><label>Business name<input name="name" defaultValue={profile.name} required/></label><label>Business phone<input name="phone" defaultValue={profile.phone||""} inputMode="tel"/></label><label>Support email<input name="supportEmail" type="email" defaultValue={profile.support_email||""}/></label><label>Website<input name="website" type="url" defaultValue={profile.website||""} placeholder="https://"/></label></div></div><div className="form-section"><p className="kicker">Primary location</p><div className="form-grid"><label>Location name<input name="locationName" defaultValue={location?.name||"Main location"}/></label><label>Street address<input name="addressLine1" defaultValue={location?.address_line1||""} required/></label><label>Address line 2<input name="addressLine2" defaultValue={location?.address_line2||""}/></label><label>City<input name="city" defaultValue={location?.city||""} required/></label><label>State<input name="state" defaultValue={location?.state||"KY"} required/></label><label>ZIP code<input name="postalCode" defaultValue={location?.postal_code||""} required/></label></div></div><div className="form-section"><p className="kicker">Commit defaults</p><div className="form-grid"><label>Timezone<select name="timezone" defaultValue={profile.timezone||"America/Chicago"}><option>America/Chicago</option><option>America/New_York</option><option>America/Denver</option><option>America/Los_Angeles</option></select></label><label>Payment-link expiration<select name="defaultExpiration" defaultValue={String(profile.default_request_expiration_minutes||30)}><option value="15">15 minutes</option><option value="30">30 minutes</option><option value="60">1 hour</option><option value="120">2 hours</option><option value="1440">24 hours</option></select></label></div></div>{error&&<p className="form-error">{error}</p>}<button className="button button-primary" disabled={busy}>{busy?"Saving…":submitLabel}</button></form>}
