"use client";
import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";
export function BusinessOnboardingForm({name,phone}:{name:string;phone:string|null}){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=new FormData(e.currentTarget);const r=await fetch("/api/merchant/business",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const result=await r.json();setBusy(false);if(!r.ok)return setError(result.error||"Unable to save.");router.push(result.next);router.refresh();}return <form className="merchant-form" onSubmit={submit}><label>Business name<input name="name" defaultValue={name} required/></label><label>Business phone<input name="phone" defaultValue={phone||""}/></label><label>Website<input name="website" type="url" placeholder="https://"/></label><label>Timezone<select name="timezone" defaultValue="America/Chicago"><option>America/Chicago</option><option>America/New_York</option><option>America/Denver</option><option>America/Los_Angeles</option></select></label>{error&&<p className="form-error">{error}</p>}<button className="button button-primary" disabled={busy}>{busy?"Saving…":"Complete business setup"}</button></form>}
