# Commit Login Redirect Update

The public website now sends Commit login traffic to:

`https://commit.vordali.com/entry`

The Commit application then determines whether the visitor needs to sign in,
create a workspace, or enter the dashboard.
