import { Suspense } from "react";import { PageShell } from "@/components/PageShell";import { RegisterForm } from "@/components/merchant/AuthForm";
export const metadata={title:"Create your account"};export default function Page(){return <PageShell><main className="merchant-auth-page"><section className="merchant-auth-copy"><p className="kicker">Merchant enrollment</p><h1>Create your Vordali Commit account.</h1><p>Choose your plan, create the business account, then continue to secure subscription checkout.</p><ol><li className="active">Account</li><li>Subscription</li><li>Business setup</li><li>Dashboard</li></ol></section><Suspense fallback={<p>Loading…</p>}><RegisterForm/></Suspense></main></PageShell>}
